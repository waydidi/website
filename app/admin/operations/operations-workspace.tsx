"use client";

import { BookOpen, CalendarDays, Camera, Check, CheckCircle2, Clipboard, ExternalLink, KeyRound, LoaderCircle, MapPin, MapPinned, Navigation, Phone, Plus, RefreshCw, Route, ShieldCheck, Truck, UserRoundPlus, X, XCircle } from "lucide-react";
import { FormEvent, useCallback, useEffect, useMemo, useState } from "react";
import { WaydidiLogo } from "@/components/waydidi-logo";

type Booking = { reference:string;customerName:string;customerEmail:string;customerPhone:string;pickup:string;dropoff:string;pickupDate:string;pickupTime:string;passengers:number;luggage:number;vehicle:string;total:number;status:string };
type Driver = { id:string;fullName:string;phone:string;email:string|null;status:string };
type Assignment = { id:string;bookingReference:string;driverId:string;currentStatus:string;assignedAt:string;tokenExpiresAt:string;revokedAt:string|null };
type DriverEvent = { id:string;assignmentId:string;bookingReference:string;status:string;latitude:number|null;longitude:number|null;accuracyMetres:number|null;expectedDistanceMetres:number|null;driverNote:string|null;evidenceKey:string|null;verificationStatus:string;rejectionReason:string|null;createdAt:string };
type Data = { bookings:Booking[];drivers:Driver[];assignments:Assignment[];events:DriverEvent[] };

const statusLabel:Record<string,string>={assigned:"Assigned",going_to_standby:"Going to standby",standby:"Standing by",passenger_picked_up:"Passenger picked up",completed:"Drop-off completed"};
const statusStyle:Record<string,string>={assigned:"bg-slate-100 text-slate-700",going_to_standby:"bg-blue-100 text-blue-800",standby:"bg-amber-100 text-amber-800",passenger_picked_up:"bg-purple-100 text-purple-800",completed:"bg-emerald-100 text-emerald-800"};

export default function OperationsWorkspace({email}:{email:string}) {
  const [data,setData]=useState<Data|null>(null); const [error,setError]=useState(""); const [busy,setBusy]=useState(""); const [selected,setSelected]=useState(""); const [filter,setFilter]=useState("active"); const [driverName,setDriverName]=useState(""); const [driverPhone,setDriverPhone]=useState(""); const [driverEmail,setDriverEmail]=useState(""); const [assignDriver,setAssignDriver]=useState<Record<string,string>>({}); const [latestLink,setLatestLink]=useState<{reference:string;url:string}|null>(null);
  const load=useCallback(async()=>{setError("");try{const response=await fetch("/api/admin/operations",{cache:"no-store"});const result=await response.json() as Data&{error?:string};if(!response.ok)throw new Error(result.error??"Operations data unavailable.");setData(result);if(!selected&&result.bookings[0])setSelected(result.bookings[0].reference);}catch(cause){setError(cause instanceof Error?cause.message:"Operations data unavailable.");}},[selected]);
  useEffect(()=>{load();const timer=window.setInterval(load,20_000);return()=>window.clearInterval(timer);},[load]);
  async function action(payload:Record<string,string>){setBusy(payload.action);setError("");try{const response=await fetch("/api/admin/operations",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const result=await response.json() as {error?:string;driverUrl?:string};if(!response.ok)throw new Error(result.error??"Action failed.");if(result.driverUrl)setLatestLink({reference:payload.bookingReference??data?.assignments.find(item=>item.id===payload.assignmentId)?.bookingReference??"Booking",url:result.driverUrl});await load();return true;}catch(cause){setError(cause instanceof Error?cause.message:"Action failed.");return false;}finally{setBusy("");}}
  async function createDriver(event:FormEvent){event.preventDefault();if(await action({action:"create_driver",fullName:driverName,phone:driverPhone,email:driverEmail})){setDriverName("");setDriverPhone("");setDriverEmail("");}}
  const activeAssignments=useMemo(()=>new Map((data?.assignments??[]).filter(item=>!item.revokedAt).map(item=>[item.bookingReference,item])),[data]);
  const rows=(data?.bookings??[]).filter(booking=>booking.status==="confirmed"||booking.status==="completed").filter(booking=>{const assignment=activeAssignments.get(booking.reference);if(filter==="unassigned")return !assignment;if(filter==="review")return (data?.events??[]).some(event=>event.bookingReference===booking.reference&&event.verificationStatus==="pending_review");if(filter==="completed")return assignment?.currentStatus==="completed";return assignment?.currentStatus!=="completed";});
  const selectedBooking=data?.bookings.find(item=>item.reference===selected); const selectedAssignment=selectedBooking?activeAssignments.get(selectedBooking.reference):undefined; const selectedDriver=data?.drivers.find(item=>item.id===selectedAssignment?.driverId); const selectedEvents=(data?.events??[]).filter(item=>item.bookingReference===selected).sort((a,b)=>a.createdAt.localeCompare(b.createdAt));
  return <main className="min-h-screen bg-[#f3f5f8] text-[#211726]"><header className="border-b border-orange-400 bg-[#FF8A05] px-5 py-5 text-white sm:px-8"><div className="mx-auto flex max-w-[1550px] flex-wrap items-center justify-between gap-4"><div className="flex items-center gap-5"><a href="/" className="inline-flex text-white"><WaydidiLogo className="h-14 w-auto"/></a><span className="hidden h-8 w-px bg-white/30 sm:block"/><div><p className="text-xs font-bold uppercase tracking-[.14em] text-white/70">Waydidi operations</p><h1 className="text-2xl font-black">Driver control</h1></div></div><div className="flex items-center gap-2 rounded-full bg-white/15 px-4 py-2 text-sm font-bold"><ShieldCheck size={17}/>{email}</div></div></header>
  <div className="mx-auto max-w-[1550px] px-4 py-6 sm:px-8"><nav className="flex w-fit max-w-full gap-1 overflow-x-auto rounded-2xl border border-slate-200 bg-white p-1 text-sm font-bold shadow-sm"><a href="/admin/calendar" className="flex shrink-0 items-center gap-2 rounded-xl px-4 py-3 text-slate-600"><CalendarDays size={17}/>Calendar</a><a href="/admin/bookings" className="flex shrink-0 items-center gap-2 rounded-xl px-4 py-3 text-slate-600"><BookOpen size={17}/>Bookings</a><a href="/admin/operations" className="flex shrink-0 items-center gap-2 rounded-xl bg-orange-50 px-4 py-3 text-[#D96F00]"><Truck size={17}/>Booking operations</a><a href="/admin/pricing" className="flex shrink-0 items-center gap-2 rounded-xl px-4 py-3 text-slate-600"><MapPinned size={17}/>Pricing areas</a></nav>
  <section className="mt-6 grid gap-5 xl:grid-cols-[1fr_470px]"><div className="space-y-5"><div className="grid gap-4 lg:grid-cols-[1fr_380px]"><section className="rounded-[26px] border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-xl font-black">Active journeys</h2><p className="mt-1 text-sm text-slate-500">Updates refresh every 20 seconds.</p></div><button onClick={load} className="grid size-11 place-items-center rounded-full border border-slate-200" aria-label="Refresh"><RefreshCw size={18}/></button></div><div className="mt-4 flex flex-wrap gap-2">{[["active","Active"],["unassigned","Unassigned"],["review","Needs review"],["completed","Completed"]].map(([value,label])=><button key={value} onClick={()=>setFilter(value)} className={`rounded-full px-4 py-2 text-sm font-bold ${filter===value?"bg-[#211726] text-white":"bg-slate-100 text-slate-600"}`}>{label}</button>)}</div></section><form onSubmit={createDriver} className="rounded-[26px] border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-2"><UserRoundPlus className="text-[#D96F00]" size={20}/><h2 className="font-black">Add driver</h2></div><div className="mt-4 grid grid-cols-2 gap-2"><input required value={driverName} onChange={event=>setDriverName(event.target.value)} placeholder="Driver name" className="h-11 rounded-xl border border-slate-200 bg-slate-50 px-3 outline-none focus:border-[#FF8A05]"/><input required value={driverPhone} onChange={event=>setDriverPhone(event.target.value)} placeholder="Phone" className="h-11 rounded-xl border border-slate-200 bg-slate-50 px-3 outline-none focus:border-[#FF8A05]"/><input type="email" value={driverEmail} onChange={event=>setDriverEmail(event.target.value)} placeholder="Email for reminders (optional)" className="col-span-2 h-11 rounded-xl border border-slate-200 bg-slate-50 px-3 outline-none focus:border-[#FF8A05]"/></div><button disabled={Boolean(busy)} className="mt-3 flex h-11 w-full items-center justify-center gap-2 rounded-full bg-[#FF8A05] font-bold text-white"><Plus size={17}/>Add driver</button></form></div>
  {latestLink&&<div className="flex flex-wrap items-center gap-3 rounded-2xl border border-emerald-200 bg-emerald-50 p-4 text-sm text-emerald-900"><CheckCircle2 size={20}/><strong>Driver link ready for {latestLink.reference}</strong><button onClick={()=>navigator.clipboard.writeText(latestLink.url)} className="ml-auto flex items-center gap-2 rounded-full bg-emerald-700 px-4 py-2 font-bold text-white"><Clipboard size={16}/>Copy link</button><button onClick={()=>setLatestLink(null)} aria-label="Dismiss"><X size={18}/></button></div>}
  {error&&<p className="rounded-2xl bg-red-50 p-4 font-semibold text-red-700">{error}</p>}
  <section className="overflow-hidden rounded-[26px] border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[950px] text-left text-sm"><thead className="bg-slate-100 text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-4">Journey</th><th className="px-5 py-4">Pickup</th><th className="px-5 py-4">Driver</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">Evidence</th><th className="px-5 py-4">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{rows.map(booking=>{const assignment=activeAssignments.get(booking.reference);const driver=data?.drivers.find(item=>item.id===assignment?.driverId);const pending=(data?.events??[]).filter(item=>item.bookingReference===booking.reference&&item.verificationStatus==="pending_review").length;return <tr key={booking.reference} onClick={()=>setSelected(booking.reference)} className={`cursor-pointer align-top hover:bg-orange-50/40 ${selected===booking.reference?"bg-orange-50/60":""}`}><td className="px-5 py-4"><strong>{booking.reference}</strong><p className="mt-1 max-w-[260px] text-slate-500">{booking.pickup} → {booking.dropoff}</p></td><td className="px-5 py-4 font-bold">{booking.pickupDate}<p className="text-slate-500">{booking.pickupTime}</p></td><td className="px-5 py-4">{driver?<><strong>{driver.fullName}</strong><p className="text-slate-500">{driver.phone}</p></>:<span className="font-bold text-amber-700">Unassigned</span>}</td><td className="px-5 py-4">{assignment?<span className={`rounded-full px-3 py-1 text-xs font-black ${statusStyle[assignment.currentStatus]??statusStyle.assigned}`}>{statusLabel[assignment.currentStatus]??assignment.currentStatus}</span>:"—"}</td><td className="px-5 py-4">{pending?<span className="rounded-full bg-amber-100 px-3 py-1 text-xs font-black text-amber-800">{pending} to review</span>:<span className="text-slate-400">Up to date</span>}</td><td className="px-5 py-4" onClick={event=>event.stopPropagation()}>{assignment?<div className="flex gap-2"><button onClick={()=>action({action:"rotate_link",assignmentId:assignment.id})} className="rounded-full border border-slate-200 px-3 py-2 text-xs font-bold">New link</button><button onClick={()=>window.confirm("Revoke this driver link?")&&action({action:"revoke",assignmentId:assignment.id})} className="rounded-full border border-red-200 px-3 py-2 text-xs font-bold text-red-700">Revoke</button></div>:<div className="flex gap-2"><select value={assignDriver[booking.reference]??""} onChange={event=>setAssignDriver({...assignDriver,[booking.reference]:event.target.value})} className="h-9 rounded-xl border border-slate-200 px-2"><option value="">Choose driver</option>{data?.drivers.filter(item=>item.status==="active").map(item=><option key={item.id} value={item.id}>{item.fullName}</option>)}</select><button disabled={!assignDriver[booking.reference]} onClick={()=>action({action:"assign",bookingReference:booking.reference,driverId:assignDriver[booking.reference]})} className="rounded-full bg-[#FF8A05] px-4 text-xs font-bold text-white disabled:opacity-40">Assign</button></div>}</td></tr>})}{rows.length===0&&<tr><td colSpan={6} className="px-5 py-14 text-center text-slate-500">No journeys match this filter.</td></tr>}</tbody></table></div></section></div>
  <aside className="h-fit rounded-[28px] border border-slate-200 bg-white p-5 shadow-sm xl:sticky xl:top-5">{selectedBooking?<><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.14em] text-[#D96F00]">Journey details</p><h2 className="mt-2 text-2xl font-black">{selectedBooking.reference}</h2><p className="mt-1 text-sm text-slate-500">{selectedBooking.customerName} · {selectedBooking.customerPhone}</p></div><a href={`tel:${selectedBooking.customerPhone}`} className="grid size-11 place-items-center rounded-full bg-[#211726] text-white"><Phone size={18}/></a></div><div className="mt-5 rounded-2xl bg-slate-50 p-4 text-sm"><p className="flex gap-2 font-bold"><MapPin className="shrink-0 text-[#FF8A05]" size={17}/>{selectedBooking.pickup}</p><p className="my-2 ml-2 h-4 border-l-2 border-dotted border-slate-300"/><p className="flex gap-2 font-bold"><Route className="shrink-0 text-[#FF8A05]" size={17}/>{selectedBooking.dropoff}</p></div>{selectedAssignment&&<div className="mt-4 flex items-center justify-between rounded-2xl border border-orange-200 bg-orange-50 p-4"><div><p className="text-xs font-bold uppercase tracking-wider text-[#D96F00]">Assigned driver</p><p className="mt-1 font-black">{selectedDriver?.fullName}</p></div><span className={`rounded-full px-3 py-1 text-xs font-black ${statusStyle[selectedAssignment.currentStatus]??statusStyle.assigned}`}>{statusLabel[selectedAssignment.currentStatus]}</span></div>}<div className="mt-6"><h3 className="font-black">Driver confirmations</h3><div className="mt-4 space-y-4">{selectedEvents.map(event=><article key={event.id} className="rounded-2xl border border-slate-200 p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-black">{statusLabel[event.status]??event.status}</p><p className="mt-1 text-xs text-slate-500">{new Date(event.createdAt).toLocaleString("en-GB",{timeZone:"Asia/Bangkok"})}</p></div><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${event.verificationStatus==="verified"?"bg-emerald-100 text-emerald-700":event.verificationStatus==="rejected"?"bg-red-100 text-red-700":event.verificationStatus==="not_required"?"bg-slate-100 text-slate-600":"bg-amber-100 text-amber-800"}`}>{event.verificationStatus.replaceAll("_"," ")}</span></div>{event.driverNote&&<p className="mt-3 rounded-xl bg-slate-50 p-3 text-sm">{event.driverNote}</p>}<div className="mt-3 flex flex-wrap gap-2">{event.evidenceKey&&<a href={`/api/admin/evidence/${event.id}`} target="_blank" className="flex items-center gap-1.5 rounded-full border border-slate-200 px-3 py-2 text-xs font-bold"><Camera size={15}/>View picture</a>}{event.latitude!=null&&event.longitude!=null&&<a href={`https://www.google.com/maps?q=${event.latitude},${event.longitude}`} target="_blank" rel="noreferrer" className="flex items-center gap-1.5 rounded-full border border-slate-200 px-3 py-2 text-xs font-bold"><Navigation size={15}/>GPS · ±{event.accuracyMetres??"?"}m <ExternalLink size={12}/></a>}</div>{event.expectedDistanceMetres!=null&&<p className={`mt-3 text-xs font-bold ${event.expectedDistanceMetres>500?"text-red-700":"text-slate-500"}`}>{event.expectedDistanceMetres.toLocaleString()} m from scheduled location</p>}{event.verificationStatus==="pending_review"&&<div className="mt-4 grid grid-cols-2 gap-2"><button onClick={()=>action({action:"verify_event",eventId:event.id})} className="flex h-10 items-center justify-center gap-1 rounded-full bg-emerald-600 text-xs font-bold text-white"><Check size={15}/>Verify</button><button onClick={()=>{const reason=window.prompt("Reason for rejection");if(reason)action({action:"reject_event",eventId:event.id,reason});}} className="flex h-10 items-center justify-center gap-1 rounded-full bg-red-50 text-xs font-bold text-red-700"><XCircle size={15}/>Reject</button></div>}{event.rejectionReason&&<p className="mt-3 text-xs font-semibold text-red-700">Reason: {event.rejectionReason}</p>}</article>)}{selectedEvents.length===0&&<div className="rounded-2xl border border-dashed border-slate-300 p-7 text-center text-sm text-slate-500"><Truck className="mx-auto mb-3 text-slate-300"/>No driver updates yet.</div>}</div></div></>:<div className="py-16 text-center text-slate-500">Choose a journey to see its driver activity.</div>}</aside></section></div></main>;
}
